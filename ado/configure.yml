name: Configure Data Product
trigger: none
pr: none

stages:
  - stage: Configure
    jobs:
      - template: ./jobs/azure-sql-db/job.yml
      - template: ./jobs/azure-key-vault/job.yml
  - stage: "Configure_2"
    jobs:
      - template: ./jobs/azure-sql-db/job.yml
      - template: ./jobs/azure-key-vault/job.yml

